# AGENTS.md — Paper Organization 维护规则

## 1. 仓库目标

本仓库长期整理人形机器人运动智能领域的论文、项目、数据集、方法对比、复现记录与研究思考。维护时始终遵循：

**准确性 > 完整性 > 结构一致性 > 更新速度。**

不得为了填满模板而猜测作者、单位、日期、开源状态、实验结果或复现结论。无法从可靠来源确认的字段使用 `null`，并在正文标注“待核验”。

## 2. 语言与记录

- 所有面向用户的回答、仓库说明和新增代码注释使用中文。
- 新写的代码文件开头必须有详细中文介绍，说明用途、输入、输出、约束和使用方式。
- 每次实质改动都必须更新根目录 `CHANGELOG.md`。
- 每篇论文的内容变化还要更新该页末尾的“更新日志”。
- 修改完成后，说明改了哪些文件、哪些部分、理由以及实际验证范围。

## 3. 信息来源优先级

1. 原论文与补充材料。
2. 官方项目页与官方代码仓库。
3. 作者或机构主页。
4. arXiv、会议或期刊官方页面。
5. 可信的二手资料，仅可辅助理解，不可覆盖一手来源。

所有可能变化的信息（代码是否开源、仓库地址、权重、数据集、许可）在更新时重新核验，并记录核验日期。论文结论与个人判断必须分开书写。

## 4. 实体与分类规则

- 论文永久 ID 使用 `P0001` 起的四位连续编号，路径为 `papers/P0001-short-name/README.md`。
- 项目永久 ID 使用 `J0001`，数据集永久 ID 使用 `D0001`。
- 一篇论文只能有一个 `primary_category`，但可以有多个标签。
- 分类必须从 `index/README.md` 定义的八类中选择。
- 标签只能从 `TAGS.md` 选择。确需新标签时，先说明理由并更新词表。
- 不因分类或题目变化移动已有实体；永久 ID 与目录保持稳定。
- `papers/*/README.md` 的 YAML front matter 是论文索引的唯一数据源，不手工编辑自动生成的表格。

## 5. 单篇论文最低要求

每个论文档案至少包含：中英文题目、日期/年份、作者、单位、出版信息、论文/项目/代码链接、分项开源状态、主分类、标签、一句话总结、研究问题、方法流程、输入输出、训练与推理、实验与结论、优点、局限、与相关工作的关系、对个人研究的价值、阅读/复现状态、本地材料和来源。

框架图若未获得可公开复用许可，不复制论文截图到公开仓库。优先使用自己重绘的图；暂缺时保留明确占位和原论文图号说明。

## 6. 本地档案与版权

- 原论文、全文翻译、方法详解和私人实验材料放在 `local_archive/<ID>/`。
- `local_archive/` 默认被 Git 忽略，只保留说明文件。
- 不把出版社 PDF、完整论文翻译、私人证件、合同或其他无关个人材料提交到公开仓库。
- 论文页可以记录仓库内相对路径，不写易失效的绝对路径。

## 7. 状态与证据边界

- 阅读状态：`unread`、`skimmed`、`read`、`deep-read`。
- 复现状态：`not-started`、`environment-ready`、`demo-tested`、`training-tested`、`sim2sim-tested`、`real-robot-tested`。
- PDF/DOCX 中写有“复现”不等于当前机器已验证；只能记录为材料中的陈述。
- 格式校验通过不代表方法正确、代码可运行、训练有效或实机安全。
- 未实际运行代码时，不得勾选 Demo、训练、Sim2Sim 或实机验证。

## 8. 标准维护流程

1. 检查 Git 状态，保护用户已有未提交工作。
2. 查重：题名、arXiv ID、DOI、项目页和 GitHub URL。
3. 分配下一个永久 ID，复制模板。
4. 读取本地材料，并用一手公开来源核验元数据和开源状态。
5. 将受版权约束材料复制到 `local_archive/<ID>/`，保持原文件名并生成清单。
6. 完成论文正文，明确“论文事实”“材料解读”“个人判断”。
7. 运行 `python3 scripts/validate_library.py --strict-local`。
8. 运行 `python3 scripts/build_index.py`，随后运行 `python3 scripts/build_index.py --check`。
9. 运行 `git diff --check`，检查变更统计并更新 `CHANGELOG.md`。
10. 仅暂存本次任务范围内的文件，使用中文提交说明创建提交。
11. 将提交推送到当前分支；若推送失败，保留本地提交并在交付说明中报告原因。

## 9. Git 规则

- 不执行 `git reset --hard`、`git clean`、强制推送或覆盖式 checkout。
- 不删除、回滚、暂存或改写用户已有变更。
- 用户已长期授权：本仓库每次完成修改并通过必要验证后，默认创建提交并推送到当前分支，无需逐次询问。
- 提交说明、变更日志、代码注释和最终交付描述均使用中文；推荐格式：`论文(P0001)：新增 XXX 精读档案`、`索引：重建论文分类页`、`文档：更新标签规范`。
- 默认授权仅覆盖本次任务产生且已验证的修改，不得混入用户原有或其他任务的未提交内容。
- 合并、变基、打标签、删除分支、强制推送及其他可能改写历史的操作仍需用户单独明确授权。

## 10. 自动生成内容

以下内容由 `scripts/build_index.py` 生成或替换，禁止手工修改自动区块：

- 根 `README.md` 的馆藏统计和最近更新区块。
- `papers/README.md`。
- `index/*.md` 的论文表格。
